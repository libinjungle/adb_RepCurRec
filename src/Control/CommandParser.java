package Control;

/**
 * Given a test file, parse command line by line. Each line can be parsed 
 * as multiple transaction that runs concurrently(Assign the same tick).
 * 
 * @author BINLI
 *
 */
public class CommandParser {
  
}
